import React from "react";

const Home = () => {
    return (
        <div style={{ padding: "2rem" }}>
            <h1>Welcome to the REST vs GraphQL Demo</h1>
            <p>
                Explore the differences between REST and GraphQL APIs. Use the navigation to try out sample queries and see responses.
            </p>
        </div>
    );
};

export default Home;